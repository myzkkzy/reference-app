<!-- 記載枠の例。ID・保存先・状態・管理担当・分類は、呼び出し元の適用規則に合わせる。 -->
<!-- 文書形式が指定されている場合は、その形式スキルでメタデータを適用する。OKFの場合はファイル先頭にfrontmatterを置き、種別・追加項目はプロジェクト規則から取得する。指定がなければ追加しない。このコメントは成果物から除去する。 -->

# {{設計ID}}：テスト方針

<!-- 保存先：適用規則で指定されたテスト方針の保存先。
設計時に方針を作り、主要設計が定まった段階から具体的計画を検証担当へ引き継ぐ。 -->

- 設計状態：ドラフト
- 目的・範囲：{{対象と対象外}}
- 入力確認日：{{YYYY-MM-DD}}
- 参照要件：{{実在する全要件ID・本文への相対リンク・内容要約・合意状況・受入条件}}
- 参照要求：{{要件の根拠となる要求ID・相対リンク}}
- 依存設計：{{実在するID・本文リンク。なければ「なし」}}
- 関連ADR：{{実在するADR-ID・相対リンク。なければ「なし」}}
- 分割・統合・廃止：{{該当時のみ新旧ID・理由・後継へのリンク}}

<!-- 設計は未使用のプロジェクト内連番。移動・編集で維持し廃止IDは再利用しない。
評価待ち・保留の解消後はドラフトとして再確認。意味に影響する変更は関連設計だけドラフトへ戻す。
状態はドラフト／評価待ち／実装引継ぎ可能／保留／廃止。レビュー判断は要件合意・ADR採用・試験合格と別。 -->

## 層別の確認目的

| テスト層 | 対象要件・受入条件・設計 | 確認目的・対象境界 | 観測・制御方法 | 検証担当への引継ぎ |
| --- | --- | --- | --- | --- |
| ユニット | {{本文リンク}} | {{単独の責務・不変条件}} | {{依存の差替境界・時計/ファイル等の制御}} | {{ケース・境界値・データ・期待結果}} |
| インテグレーション | {{本文リンク}} | {{組合せ・実物の依存先・データ/エラー/順序契約}} | {{結果と内部状態の観測・失敗を制御する境界}} | {{環境・準備/後片付け・失敗再現}} |
| E2E | {{本文リンク}} | {{主要な利用経路・対象境界}} | {{入力・状態・結果の観測}} | {{シナリオ・操作手順・期待結果・環境}} |
| 性能・配布 | {{本文リンク}} | {{品質・配布条件}} | {{計測点・完了判定・負荷状態の検出}} | {{固定データ・状態・環境・手順・反復実行}} |

{{層の分担理由。重複が必要な場合は目的の違いを記載し、同じケースを機械的に増やさない}}

## データと処理の検証観点

| 観点 | 根拠要件・受入条件・設計 | 確認目的・層 | 観測・引継ぎ |
| --- | --- | --- | --- |
| 要件と振る舞い | {{要件本文リンク}} | {{操作・結果が要件の意味と一致}} | {{確認対象}} |
| データ整合性・更新単位 | {{本文リンク}} | {{更新単位内の保証と失敗時の保護}} | {{状態・依存の制御方法}} |
| データ変換・外部連携 | {{該当時のみ}} | {{意味の対応と技術契約}} | {{正常・異常の観測境界}} |

{{該当しない行は削除。既存の層別方針とケースを重複させず、具体的ケース・コード・実行は後続担当へ}}

## 品質評価の前提

| 根拠要件 | 確定済みの閾値・負荷・試行回数・合格条件 | 未決の評価条件 | 解消条件・時期・役割 |
| --- | --- | --- | --- |
| {{本文リンク}} | {{上流の値を維持}} | {{値を捏造しない}} | {{必要な本人合意と上流反映を含む}} |

## 観測可能性と依存の扱い

{{実物を使う境界、依存の差替・失敗制御方法、状態・結果の観測、計測開始/完了点。
テスト不能な構造・観測不足は対象設計へ返す。具体的なテストコードは作らない}}

## 検証計画との接続

{{存在する検証計画へのリンク。未作成なら「未作成」と引継ぎ先の役割}}

検証担当が 適用規則で指定された検証文書の保存先 に具体的なケース・データ・期待結果・環境・手順、自動化範囲、テストダブルの使用法、実行タイミング、失敗判定・再実行方針を記録する。要件・受入条件・設計を関連付け、結果には対象ビルド・環境・データ・手順・結果への参照を残す。設計担当はその保存先を更新しない。テストコードは実装担当、実行・評価は検証担当が担う。

{{要件の評価条件が未確定なら最終受入を判定しない。予備計測と受入結果を区別する}}

## 未決事項・引継ぎ

{{manage-product-documents の handoff.md の記載枠をここへ挿入。なければ「なし」。別台帳へ複製しない}}

## 完了確認

- 対象要件・受入条件への対応：{{本文への参照または不足}}
- 主要構造・データ/インターフェース契約・正常異常処理：{{具体化した範囲と不足}}
- 図・本文・採用ADRの整合：{{確認内容}}
- テスト方針・観測方法・引継ぎ：{{関連設計・本文リンクと確認内容}}
- 実装を左右する未決・必要な試作根拠：{{解消と証拠、または残る影響}}

- レビュー判断：{{確認日・担当・状態の根拠。局所的な実装判断として残す事項}}

<!-- 必要な試作結果を確認してから実装引継ぎ可能にする。将来の受入試験合格自体は前提にしない。
条件付き合意の進行範囲を越える実装・最終受入の許可を推定しない。 -->
